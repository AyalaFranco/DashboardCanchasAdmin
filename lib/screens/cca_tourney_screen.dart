import 'package:flutter/material.dart';

class CCATourneyScreen extends StatelessWidget {
  const CCATourneyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PANTALLA TORNEOS"));
  }
}