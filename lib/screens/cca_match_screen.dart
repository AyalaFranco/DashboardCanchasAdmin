import 'package:flutter/material.dart';

class CCAMatchScreen extends StatelessWidget {
  const CCAMatchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("PANTALLA SUMO UNO"));
  }
}