import 'package:flutter/material.dart';

class CCAProfileScreen extends StatelessWidget {
  const CCAProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("PERFIL"),), body: const Center(child: Text("PANTALLA PERFIL")));
  }
}
